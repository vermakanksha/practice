#Exercise 3
X1<-26:35
X2<-10:1
mean(X1)
mean(X2)
median(X1)
median(X2)
summary(X1)
summary(X2)
sd(X1)
sd(X2)
cor(X1,X2)
var(X1)
var(X2)
range(X1)
range(X2)
install.packages("moments")
library(moments)
skewness(X1)
skewness(X2)
kurtosis(X1)
kurtosis(X2)
moment(X1)
moment(X2)
quantile(X1)
quantile(X2)
#Exercise 4
str(mtcars)
summary(mtcars)
nrow(mtcars)
ncol(mtcars)
names(mtcars)
str(mtcars)
range(mtcars$mpg)
range(mtcars$cyl)
range(mtcars$disp)
range(mtcars$hp)
head(mtcars)
tail(mtcars)
table(mtcars$mpg)
table(mtcars$cyl)
barplot(table(mtcars$mpg))
barplot(table(mtcars$cyl))
summary(mtcars$mpg)
summary(mtcars$hp)
summary(mtcars$disp)
cor(mtcars$hp,mtcars$disp)
summary(mtcars$wt)
cor(mtcars$wt,mtcars$gear)
plot(cor(mtcars$wt,mtcars$gear))
summary(mtcars)
by(mtcars,mtcars$mpg,summary)
summary(mtcars$mpg)
by(mtcars$mpg,mtcars$am,mean)
mtcars[order(mtcars$mpg,decreasing=T),]
by(mtcars,mtcars$mpg,order(decreasing=T))
sort(mtcars$drat)